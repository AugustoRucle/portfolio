import RandomLetters from "./RandomLetters";
import { defaultProps, propTypes } from './propTypes';

RandomLetters.propTypes = propTypes;
RandomLetters.defaultProps = defaultProps;

export default RandomLetters;